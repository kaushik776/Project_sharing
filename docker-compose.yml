version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"  # Maps container port 5000 to localhost:5000
    volumes:
      - ./cache:/app/cache
    environment:
      - FASTF1_CACHE_DIR=/app/cache